machine:
  node:
    version: 8.10.0

  environment:
    YARN_VERSION: 0.27.5
    PATH: "${PATH}:${HOME}/.yarn/bin:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

test:
  override:
    - npm run test-ci
